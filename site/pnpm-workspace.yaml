# Single-project pnpm root (separate from the extension/server monorepo above).
# Not a workspace — no `packages:`; just allow the native build tools' install scripts.
allowBuilds:
  esbuild: true
  "@tailwindcss/oxide": true
